# Secrets
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/settings.json
.idea/

# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Build artifacts
dist/
build/
*.zip

# Claude Code
.claude/settings.local.json
.claude/launch.json

# Project versions (snapshot folders inside projects)
**/09_ДЕПЛОЙ/versions/

# Logs
*.log
wiki/.obsidian/
logs/wiki-usage.jsonl
wiki/routing-report.md

# Wiki run tracking
.wiki-run-id

# PR-O block-import work cache
.import-blocks-work/

# Audit reports are PUBLIC (audit/REPORT.md, REPORT-v2.md, zone details)
# Only ignore the auto-generated legacy bypass log
audit/legacy-bypass.log

.claude/worktrees/

.cleanup-staging/

# Claude Code scheduler lock (local)
.claude/scheduled_tasks.lock

# iCloud/merge-дубликаты «<name> 2.ext» — байт-в-байт копии оригиналов.
# Не индексируем; pytest их тоже игнорит (pytest.ini --ignore-glob).
# Удалить локально: find . -name "* 2.*" -not -path "./.git/*"  (только untracked).
* 2.*
*\ 2.*
.worktrees/
