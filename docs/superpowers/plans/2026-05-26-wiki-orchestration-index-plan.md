# Wiki as Orchestration-Index Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace "orchestrator reads big source files" with "orchestrator queries wiki via CLI and reads 3-5 short cards" — reducing token spend and Claude Code sessions per pipeline run.

**Architecture:** Two-tier wiki. Machine-readable `wiki/index.yaml` (built deterministically from card frontmatter) is the source of truth for orchestrator routing. Cards in `wiki/concepts/**/*.md` carry full contracts (frontmatter + fixed body sections) and are auto-generated by SDK from sources. Markdown `wiki/index.md` is regenerated from YAML for human readers. A pure-Python CLI (`scripts/wiki/query.py`) filters the index without LLM calls; the SessionStart hook injects only a ~50-token hint instead of the whole index.

**Tech Stack:** Python 3 (no new runtime deps), PyYAML (already used in `utils.py`), claude-agent-sdk (already used in `sdk_client.py`), pytest + pytest-mock for tests.

---

## Pre-flight Check

Before starting Task 1, verify environment:

- [ ] **Step 1: Confirm pytest-mock is installed**

Run: `python -m pip show pytest-mock`
Expected: package info shown (was installed earlier in the session).

- [ ] **Step 2: Confirm working tree clean except for plan/spec files**

Run: `git status --short`
Expected: only untracked items (`.worktrees/`, `.tmp-*.md`) — no staged or modified source files.

- [ ] **Step 3: Confirm spec exists**

Run: `ls docs/superpowers/specs/2026-05-26-wiki-orchestration-index-design.md`
Expected: file exists (committed in `2cd251a`).

---

## Stage 1 — Schema & Lint Foundation (no behavior change yet)

### Task 1: Add `parse_frontmatter_full()` to utils

**Files:**
- Modify: `scripts/wiki/utils.py`
- Test: `tests/wiki/test_utils_frontmatter.py` (create)

**Rationale:** Current `parse_frontmatter()` returns `(meta, body)`. We need a version that returns the **full** metadata dict including all card fields (tags, triggers, gates, pre_reqs, related, inputs, outputs, confidence, incomplete) — used by `_build_yaml_index()` later. We keep `parse_frontmatter()` untouched for backward compat.

- [ ] **Step 1: Write the failing test**

Create `tests/wiki/test_utils_frontmatter.py`:

```python
"""Tests for parse_frontmatter_full — extracts full card metadata."""
import pytest
from scripts.wiki import utils


def test_parse_frontmatter_full_extracts_all_known_fields():
    text = """---
slug: 08-kod
type: stage
stage: "08"
name: "Сборка"
tags: [wp, gutenberg]
triggers: [approve-07b]
inputs: [composed.html]
outputs: [wp-theme/]
gates: [composed_approved]
pre_reqs: [05-design]
related: [landing-build]
confidence:
  gates: low
---
body here
"""
    meta = utils.parse_frontmatter_full(text)
    assert meta["slug"] == "08-kod"
    assert meta["type"] == "stage"
    assert meta["stage"] == "08"
    assert meta["tags"] == ["wp", "gutenberg"]
    assert meta["triggers"] == ["approve-07b"]
    assert meta["inputs"] == ["composed.html"]
    assert meta["outputs"] == ["wp-theme/"]
    assert meta["gates"] == ["composed_approved"]
    assert meta["pre_reqs"] == ["05-design"]
    assert meta["related"] == ["landing-build"]
    assert meta["confidence"] == {"gates": "low"}


def test_parse_frontmatter_full_returns_empty_on_no_frontmatter():
    assert utils.parse_frontmatter_full("just body") == {}


def test_parse_frontmatter_full_returns_empty_on_malformed_yaml():
    text = "---\nslug: [unclosed\n---\nbody"
    assert utils.parse_frontmatter_full(text) == {}


def test_parse_frontmatter_full_unknown_fields_preserved():
    text = """---
slug: x
custom_field: value
---
body
"""
    meta = utils.parse_frontmatter_full(text)
    assert meta["custom_field"] == "value"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/wiki/test_utils_frontmatter.py -v`
Expected: `AttributeError: module 'scripts.wiki.utils' has no attribute 'parse_frontmatter_full'`

- [ ] **Step 3: Add `parse_frontmatter_full` to `scripts/wiki/utils.py`**

Append after the existing `parse_frontmatter` function:

```python
def parse_frontmatter_full(text: str) -> dict[str, Any]:
    """Извлекает полный YAML frontmatter как dict без отделения body.

    Returns:
        dict с полным набором полей. Если frontmatter нет или повреждён — {}.
    """
    meta, _ = parse_frontmatter(text)
    return meta
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/wiki/test_utils_frontmatter.py -v`
Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add scripts/wiki/utils.py tests/wiki/test_utils_frontmatter.py
git commit -m "feat(wiki): add parse_frontmatter_full for full card metadata"
```

---

### Task 2: Add `_build_yaml_index()` to system_compiler

**Files:**
- Modify: `scripts/wiki/system_compiler.py`
- Test: `tests/wiki/test_yaml_index.py` (create)

**Rationale:** Build `wiki/index.yaml` deterministically from `concepts_summary` (the in-memory list built during compile). Pure Python, no SDK. Schema version constant lets us evolve format later.

- [ ] **Step 1: Write the failing test**

Create `tests/wiki/test_yaml_index.py`:

```python
"""Tests for _build_yaml_index — deterministic YAML index builder."""
import yaml

from scripts.wiki import system_compiler


def test_build_yaml_index_basic_structure():
    concepts = [
        {
            "slug": "08-kod",
            "type": "stage",
            "stage": "08",
            "name": "Сборка",
            "tags": ["wp"],
            "triggers": ["approve-07b"],
            "card": "concepts/stages/08-kod.md",
            "source": "template/08_КОД/README.md",
        },
        {
            "slug": "block-composer",
            "type": "agent",
            "name": "block-composer",
            "tags": ["html"],
            "card": "concepts/agents/block-composer.md",
            "source": "agents/block-composer.md",
        },
    ]
    text = system_compiler._build_yaml_index(concepts)
    data = yaml.safe_load(text)
    assert data["version"] == 1
    assert data["counts"]["total"] == 2
    assert data["counts"]["by_type"] == {"stage": 1, "agent": 1}
    assert len(data["concepts"]) == 2
    assert data["concepts"][0]["slug"] == "08-kod"


def test_build_yaml_index_sorts_concepts_by_slug():
    concepts = [
        {"slug": "z-last", "type": "agent", "card": "a", "source": "b"},
        {"slug": "a-first", "type": "agent", "card": "a", "source": "b"},
    ]
    data = yaml.safe_load(system_compiler._build_yaml_index(concepts))
    assert [c["slug"] for c in data["concepts"]] == ["a-first", "z-last"]


def test_build_yaml_index_omits_none_fields():
    """Optional fields with None/empty values should not appear in YAML."""
    concepts = [
        {
            "slug": "x",
            "type": "agent",
            "name": "x",
            "card": "c",
            "source": "s",
            "tags": None,
            "triggers": None,
        }
    ]
    data = yaml.safe_load(system_compiler._build_yaml_index(concepts))
    c = data["concepts"][0]
    assert "tags" not in c
    assert "triggers" not in c


def test_build_yaml_index_preserves_confidence_and_incomplete():
    concepts = [
        {
            "slug": "x",
            "type": "stage",
            "stage": "08",
            "card": "c",
            "source": "s",
            "confidence": {"gates": "low"},
            "incomplete": True,
        }
    ]
    data = yaml.safe_load(system_compiler._build_yaml_index(concepts))
    c = data["concepts"][0]
    assert c["confidence"] == {"gates": "low"}
    assert c["incomplete"] is True


def test_build_yaml_index_has_schema_version_constant():
    assert system_compiler.INDEX_SCHEMA_VERSION == 1
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/wiki/test_yaml_index.py -v`
Expected: `AttributeError: module 'scripts.wiki.system_compiler' has no attribute '_build_yaml_index'`

- [ ] **Step 3: Add `_build_yaml_index` and schema version**

In `scripts/wiki/system_compiler.py`, add after the existing `_build_index` function:

```python
INDEX_SCHEMA_VERSION = 1

_YAML_OPTIONAL_FIELDS = (
    "stage", "tags", "triggers", "inputs", "outputs",
    "gates", "pre_reqs", "related", "invoked_by", "uses_skills",
    "confidence", "incomplete",
)

_YAML_REQUIRED_FIELDS = ("slug", "type", "name", "card", "source")


def _build_yaml_index(concepts: list[dict[str, Any]]) -> str:
    """Детерминированно строит YAML-индекс для orchestrator routing.

    Без SDK. Сортирует концепты по slug. Опциональные поля с None/пустыми
    значениями пропускаются, чтобы YAML был компактным.
    """
    from datetime import datetime, timezone
    import yaml as _yaml

    by_type: dict[str, int] = {}
    cleaned: list[dict[str, Any]] = []
    for c in sorted(concepts, key=lambda x: x.get("slug", "")):
        entry: dict[str, Any] = {}
        for field in _YAML_REQUIRED_FIELDS:
            value = c.get(field)
            if value is not None:
                entry[field] = value
        for field in _YAML_OPTIONAL_FIELDS:
            value = c.get(field)
            if value is None:
                continue
            if isinstance(value, (list, dict)) and len(value) == 0:
                continue
            entry[field] = value
        cleaned.append(entry)
        type_ = c.get("type", "unknown")
        by_type[type_] = by_type.get(type_, 0) + 1

    doc = {
        "version": INDEX_SCHEMA_VERSION,
        "generated_at": datetime.now(timezone.utc).isoformat(timespec="seconds"),
        "counts": {"total": len(cleaned), "by_type": by_type},
        "concepts": cleaned,
    }
    return _yaml.safe_dump(doc, allow_unicode=True, sort_keys=False)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/wiki/test_yaml_index.py -v`
Expected: 5 passed.

- [ ] **Step 5: Commit**

```bash
git add scripts/wiki/system_compiler.py tests/wiki/test_yaml_index.py
git commit -m "feat(wiki): add _build_yaml_index for deterministic machine index"
```

---

### Task 3: Wire `_build_yaml_index` into compile flow and extend `concepts_summary`

**Files:**
- Modify: `scripts/wiki/system_compiler.py` (the `compile_system` function)
- Test: extend `tests/wiki/test_system_compiler.py`

**Rationale:** Make `compile_system()` actually emit `wiki/index.yaml` on every run, and ensure `concepts_summary` carries the extended frontmatter fields needed by the YAML builder.

- [ ] **Step 1: Write the failing test**

Append to `tests/wiki/test_system_compiler.py`:

```python
def test_compile_emits_index_yaml(fake_repo, tmp_path, mocker):
    """compile_system должен писать wiki/index.yaml с правильной структурой."""
    import yaml

    wiki = tmp_path / "wiki"
    mocker.patch(
        "scripts.wiki.system_compiler.sdk_client.generate",
        return_value=(
            "---\n"
            "slug: sample-agent\n"
            "type: agent\n"
            "name: sample-agent\n"
            "tags: [demo]\n"
            "triggers: [test-trigger]\n"
            "---\n"
            "Body."
        ),
    )
    mocker.patch(
        "scripts.wiki.system_compiler._build_index",
        return_value="# Index stub",
    )
    sources = [{"path": "agents/*.md", "concept_dir": "agents"}]

    system_compiler.compile_system(
        repo_root=fake_repo, wiki_dir=wiki, sources=sources
    )

    index_yaml = wiki / "index.yaml"
    assert index_yaml.exists()
    data = yaml.safe_load(index_yaml.read_text(encoding="utf-8"))
    assert data["version"] == 1
    assert data["counts"]["total"] == 1
    concept = data["concepts"][0]
    assert concept["slug"] == "sample-agent"
    assert concept["type"] == "agent"
    assert concept["tags"] == ["demo"]
    assert concept["triggers"] == ["test-trigger"]
    assert concept["card"] == "concepts/agents/sample-agent.md"


def test_concepts_summary_carries_extended_frontmatter(fake_repo, tmp_path, mocker):
    """После compile concepts_summary должен содержать tags/triggers/related."""
    wiki = tmp_path / "wiki"
    mocker.patch(
        "scripts.wiki.system_compiler.sdk_client.generate",
        return_value=(
            "---\n"
            "slug: sample-agent\n"
            "type: agent\n"
            "name: sample-agent\n"
            "tags: [a, b]\n"
            "related: [x, y]\n"
            "---\n"
            "body"
        ),
    )
    mocker.patch(
        "scripts.wiki.system_compiler._build_index",
        return_value="idx",
    )
    sources = [{"path": "agents/*.md", "concept_dir": "agents"}]
    system_compiler.compile_system(repo_root=fake_repo, wiki_dir=wiki, sources=sources)

    import yaml
    data = yaml.safe_load((wiki / "index.yaml").read_text(encoding="utf-8"))
    concept = data["concepts"][0]
    assert concept["tags"] == ["a", "b"]
    assert concept["related"] == ["x", "y"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/wiki/test_system_compiler.py::test_compile_emits_index_yaml -v`
Expected: FAIL — `wiki/index.yaml` does not exist.

- [ ] **Step 3: Extend `concepts_summary` in `compile_system`**

In `scripts/wiki/system_compiler.py`, find the two places that build `concepts_summary` entries (in the skipped branch and in the compiled branch — search for `concepts_summary.append`). Replace both blocks with this helper call:

First, add a module-level helper above `compile_system`:

```python
def _summary_entry_from_meta(
    slug: str, meta: dict[str, Any], rel_key: str, concept_dir: str
) -> dict[str, Any]:
    """Превращает frontmatter карточки в запись для index.yaml."""
    return {
        "slug": slug,
        "type": meta.get("type", "unknown"),
        "stage": meta.get("stage"),
        "name": meta.get("name", slug),
        "tags": meta.get("tags"),
        "triggers": meta.get("triggers"),
        "inputs": meta.get("inputs"),
        "outputs": meta.get("outputs"),
        "gates": meta.get("gates"),
        "pre_reqs": meta.get("pre_reqs"),
        "related": meta.get("related"),
        "invoked_by": meta.get("invoked_by"),
        "uses_skills": meta.get("uses_skills"),
        "confidence": meta.get("confidence"),
        "incomplete": meta.get("incomplete"),
        "card": f"concepts/{concept_dir}/{slug}.md",
        "source": rel_key,
    }
```

Then in the skipped branch (around the line `if not hash_cache.is_changed(...)`):

```python
            if not hash_cache.is_changed(source_path, rel_key, cache):
                skipped.append(rel_key)
                if concept_path.exists():
                    meta, _ = utils.parse_frontmatter(
                        concept_path.read_text(encoding="utf-8")
                    )
                    concepts_summary.append(
                        _summary_entry_from_meta(slug, meta, rel_key, concept_dir)
                    )
                continue
```

And in the compiled branch (after `meta, _ = utils.parse_frontmatter(content)`):

```python
            meta, _ = utils.parse_frontmatter(content)
            concepts_summary.append(
                _summary_entry_from_meta(slug, meta, rel_key, concept_dir)
            )
```

- [ ] **Step 4: Wire YAML index emission into `compile_system`**

In `scripts/wiki/system_compiler.py`, locate the `# Индекс` section near the end of `compile_system`. Replace it with:

```python
    # YAML index — машинный, всегда детерминирован
    if not dry_run and concepts_summary:
        yaml_index_content = _build_yaml_index(concepts_summary)
        utils.atomic_write(wiki_dir / "index.yaml", yaml_index_content)

    # Markdown index — для людей
    if concepts_summary:
        try:
            index_content = _build_index(concepts_summary)
            if not dry_run:
                utils.atomic_write(wiki_dir / "index.md", index_content)
        except sdk_client.SDKError as e:
            errors.append(f"index: {e}")
```

- [ ] **Step 5: Run all system_compiler tests**

Run: `python -m pytest tests/wiki/test_system_compiler.py -v`
Expected: all tests pass including the two new ones.

- [ ] **Step 6: Commit**

```bash
git add scripts/wiki/system_compiler.py tests/wiki/test_system_compiler.py
git commit -m "feat(wiki): emit index.yaml on compile + extended frontmatter in summary"
```

---

### Task 4: Add lint rules for broken refs / dup slugs / missing fields

**Files:**
- Modify: `scripts/wiki/lint.py`
- Test: `tests/wiki/test_lint_refs.py` (create)

**Rationale:** Existing lint checks `[[wikilinks]]` in body text. We need new checks on **frontmatter references** (related, triggers, pre_reqs) and structural rules (dup slug, missing required field for type). These run from `compile.py` too so broken refs auto-fail the build (unless `WIKI_LINT_STRICT=0`).

- [ ] **Step 1: Write the failing test**

Create `tests/wiki/test_lint_refs.py`:

```python
"""Tests for new lint rules on index.yaml frontmatter references."""
import yaml
import pytest

from scripts.wiki import lint


def _make_index(tmp_path, concepts: list[dict]) -> dict:
    """Helper: write index.yaml and return parsed data."""
    wiki = tmp_path / "wiki"
    wiki.mkdir()
    data = {
        "version": 1,
        "concepts": concepts,
    }
    (wiki / "index.yaml").write_text(
        yaml.safe_dump(data, allow_unicode=True), encoding="utf-8"
    )
    return data


def test_check_index_refs_detects_broken_related(tmp_path):
    data = _make_index(tmp_path, [
        {"slug": "a", "type": "agent", "name": "a", "card": "c", "source": "s",
         "related": ["does-not-exist"]},
    ])
    issues = lint.check_index_refs(data)
    assert any("does-not-exist" in i for i in issues["broken_refs"])


def test_check_index_refs_detects_duplicate_slug(tmp_path):
    data = _make_index(tmp_path, [
        {"slug": "dup", "type": "agent", "name": "a", "card": "c", "source": "s"},
        {"slug": "dup", "type": "skill", "name": "b", "card": "d", "source": "t"},
    ])
    issues = lint.check_index_refs(data)
    assert any("dup" in i for i in issues["dup_slugs"])


def test_check_index_refs_detects_missing_stage_for_stage_type(tmp_path):
    data = _make_index(tmp_path, [
        {"slug": "x", "type": "stage", "name": "x", "card": "c", "source": "s"},
    ])
    issues = lint.check_index_refs(data)
    assert any("stage field" in i for i in issues["missing_required"])


def test_check_index_refs_allows_valid_index(tmp_path):
    data = _make_index(tmp_path, [
        {"slug": "a", "type": "agent", "name": "a", "card": "c", "source": "s",
         "related": ["b"]},
        {"slug": "b", "type": "skill", "name": "b", "card": "d", "source": "t"},
    ])
    issues = lint.check_index_refs(data)
    assert all(not v for v in issues.values())


def test_check_index_refs_incomplete_concepts_skipped(tmp_path):
    """Incomplete entries (throttled, deferred) don't trigger missing-field errors."""
    data = _make_index(tmp_path, [
        {"slug": "x", "type": "stage", "name": "x", "card": "c", "source": "s",
         "incomplete": True},
    ])
    issues = lint.check_index_refs(data)
    assert not issues["missing_required"]


def test_check_index_refs_confidence_warns_not_errors(tmp_path):
    data = _make_index(tmp_path, [
        {"slug": "x", "type": "agent", "name": "x", "card": "c", "source": "s",
         "confidence": {"triggers": "low"}},
    ])
    issues = lint.check_index_refs(data)
    assert any("x" in i for i in issues["low_confidence"])
    assert not issues["broken_refs"]
    assert not issues["dup_slugs"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/wiki/test_lint_refs.py -v`
Expected: `AttributeError: module 'scripts.wiki.lint' has no attribute 'check_index_refs'`

- [ ] **Step 3: Add `check_index_refs` to `scripts/wiki/lint.py`**

Append to `scripts/wiki/lint.py` (before `def main`):

```python
# Required frontmatter fields per concept type.
_REQUIRED_FIELDS_BY_TYPE = {
    "stage": ("stage",),
    "agent": ("triggers",),
    "command": ("triggers",),
    "skill": (),
    "rule": (),
    "catalog": (),
}

# Frontmatter fields that should reference existing slugs.
_REF_FIELDS = ("related", "pre_reqs", "invoked_by", "uses_skills")


def check_index_refs(index_data: dict) -> dict[str, list[str]]:
    """Проверяет валидность ссылок и обязательных полей в index.yaml.

    Returns:
        {'broken_refs': [...], 'dup_slugs': [...], 'missing_required': [...],
         'low_confidence': [...], 'orphan_cards': []}.
    """
    issues: dict[str, list[str]] = {
        "broken_refs": [],
        "dup_slugs": [],
        "missing_required": [],
        "low_confidence": [],
        "orphan_cards": [],
    }
    concepts = index_data.get("concepts", [])
    slugs: set[str] = set()
    seen: set[str] = set()

    for c in concepts:
        slug = c.get("slug")
        if not slug:
            continue
        if slug in seen:
            issues["dup_slugs"].append(slug)
        seen.add(slug)
        slugs.add(slug)

    for c in concepts:
        slug = c.get("slug", "?")
        if c.get("incomplete"):
            continue
        type_ = c.get("type", "unknown")
        for required in _REQUIRED_FIELDS_BY_TYPE.get(type_, ()):
            if not c.get(required):
                issues["missing_required"].append(
                    f"{slug} ({type_}): missing '{required}' field"
                )
        for field in _REF_FIELDS:
            for target in c.get(field) or []:
                if target not in slugs:
                    issues["broken_refs"].append(f"{slug}.{field} → {target}")
        confidence = c.get("confidence") or {}
        for field, level in confidence.items():
            if level == "low":
                issues["low_confidence"].append(f"{slug}.{field}")

    return issues
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/wiki/test_lint_refs.py -v`
Expected: 6 passed.

- [ ] **Step 5: Wire `check_index_refs` into `run_checks`**

In `scripts/wiki/lint.py`, modify `run_checks` to also load `index.yaml` if it exists and merge `check_index_refs` results. Find the line `issues = {` near the top of `run_checks` and after building `issues`, add this at the **end** of `run_checks` just before `return issues`:

```python
    # New: index.yaml ref checks (Stage 1).
    index_yaml = wiki_dir / "index.yaml"
    if index_yaml.exists():
        import yaml as _yaml
        try:
            data = _yaml.safe_load(index_yaml.read_text(encoding="utf-8")) or {}
        except _yaml.YAMLError:
            data = {}
        ref_issues = check_index_refs(data)
        for key, items in ref_issues.items():
            issues.setdefault(key, []).extend(items)

    return issues
```

(Remove the original `return issues` line, since the new block ends with `return issues`.)

- [ ] **Step 6: Add `WIKI_LINT_STRICT` env override to `compute_exit_code`**

Replace `compute_exit_code` in `scripts/wiki/lint.py`:

```python
# Issue categories that are warn-only (don't fail compile).
_WARN_ONLY_KEYS = {"low_confidence", "stale", "missing_backlinks", "empty", "orphans"}


def compute_exit_code(issues: dict) -> int:
    """0 если только warn-only issues, 1 если есть критические.

    Override через env WIKI_LINT_STRICT=0 — всё становится warn-only.
    """
    if os.environ.get("WIKI_LINT_STRICT", "1") == "0":
        return 0
    for key, items in issues.items():
        if key in _WARN_ONLY_KEYS:
            continue
        if items:
            return 1
    return 0
```

Add `import os` at the top of `scripts/wiki/lint.py` if not already present.

- [ ] **Step 7: Run full lint suite**

Run: `python -m pytest tests/wiki/test_lint_refs.py tests/wiki/test_lint*.py -v` (last pattern catches any existing lint tests)
Expected: all green.

- [ ] **Step 8: Commit**

```bash
git add scripts/wiki/lint.py tests/wiki/test_lint_refs.py
git commit -m "feat(wiki): lint rules for index.yaml refs + WIKI_LINT_STRICT override"
```

---

## Stage 2 — CLI Query

### Task 5: Rewrite `scripts/wiki/query.py` for YAML index

**Files:**
- Modify (full rewrite): `scripts/wiki/query.py`
- Test: `tests/wiki/test_query.py` (create or replace)

**Rationale:** The existing `query.py` is an SDK-backed Q&A tool (asks Claude a free-form question with the full index.md in context). Spec replaces it with a pure-Python filter over `index.yaml` — no SDK, no tokens. This is the core token-saving piece.

- [ ] **Step 1: Write the failing test**

Create `tests/wiki/test_query.py`:

```python
"""Tests for scripts.wiki.query — pure Python filter over index.yaml."""
import json
from pathlib import Path

import yaml
import pytest

from scripts.wiki import query


@pytest.fixture
def fake_index(tmp_path) -> Path:
    """Маленький index.yaml для тестов фильтрации."""
    wiki = tmp_path / "wiki"
    wiki.mkdir()
    concepts_dir = wiki / "concepts" / "agents"
    concepts_dir.mkdir(parents=True)
    (concepts_dir / "block-composer.md").write_text(
        "---\nslug: block-composer\n---\nCard body for block-composer.\n",
        encoding="utf-8",
    )
    (concepts_dir / "frontend-builder.md").write_text(
        "---\nslug: frontend-builder\n---\nCard body for frontend-builder.\n",
        encoding="utf-8",
    )
    data = {
        "version": 1,
        "concepts": [
            {
                "slug": "block-composer", "type": "agent", "stage": "08",
                "name": "block-composer", "tags": ["wp", "gutenberg"],
                "triggers": ["landing-build"],
                "card": "concepts/agents/block-composer.md",
                "source": "agents/block-composer.md",
            },
            {
                "slug": "frontend-builder", "type": "agent", "stage": "08",
                "name": "frontend-builder", "tags": ["wp", "css"],
                "triggers": ["landing-build"],
                "card": "concepts/agents/frontend-builder.md",
                "source": "agents/frontend-builder.md",
            },
            {
                "slug": "seo-optimizer", "type": "agent", "stage": "12",
                "name": "seo-optimizer", "tags": ["seo"],
                "triggers": ["approve-deploy"],
                "card": "concepts/agents/seo-optimizer.md",
                "source": "agents/seo-optimizer.md",
            },
        ],
    }
    (wiki / "index.yaml").write_text(
        yaml.safe_dump(data, allow_unicode=True), encoding="utf-8"
    )
    return wiki


def test_filter_by_stage(fake_index):
    result = query.filter_concepts(fake_index, stage="08")
    slugs = [c["slug"] for c in result]
    assert "block-composer" in slugs
    assert "frontend-builder" in slugs
    assert "seo-optimizer" not in slugs


def test_filter_by_type(fake_index):
    result = query.filter_concepts(fake_index, type_="agent")
    assert len(result) == 3


def test_filter_by_tag(fake_index):
    result = query.filter_concepts(fake_index, tag="gutenberg")
    assert len(result) == 1
    assert result[0]["slug"] == "block-composer"


def test_filter_by_trigger(fake_index):
    result = query.filter_concepts(fake_index, trigger="landing-build")
    assert {c["slug"] for c in result} == {"block-composer", "frontend-builder"}


def test_filter_by_slug_returns_single_match(fake_index):
    result = query.filter_concepts(fake_index, slug="block-composer")
    assert len(result) == 1
    assert result[0]["slug"] == "block-composer"


def test_filter_combined(fake_index):
    result = query.filter_concepts(fake_index, stage="08", tag="css")
    assert len(result) == 1
    assert result[0]["slug"] == "frontend-builder"


def test_filter_by_grep(fake_index):
    result = query.filter_concepts(fake_index, grep="seo")
    assert len(result) == 1
    assert result[0]["slug"] == "seo-optimizer"


def test_format_compact_includes_summary(fake_index):
    concepts = query.filter_concepts(fake_index, slug="block-composer")
    text = query.format_output(fake_index, concepts, fmt="compact")
    assert "block-composer" in text
    assert "wp" in text
    assert "concepts/agents/block-composer.md" in text


def test_format_slugs(fake_index):
    concepts = query.filter_concepts(fake_index, stage="08")
    text = query.format_output(fake_index, concepts, fmt="slugs")
    lines = [l for l in text.splitlines() if l.strip()]
    assert set(lines) == {"block-composer", "frontend-builder"}


def test_format_json_parses(fake_index):
    concepts = query.filter_concepts(fake_index, stage="08")
    text = query.format_output(fake_index, concepts, fmt="json")
    parsed = json.loads(text)
    assert len(parsed) == 2


def test_format_cards_reads_card_files(fake_index):
    concepts = query.filter_concepts(fake_index, slug="block-composer")
    text = query.format_output(fake_index, concepts, fmt="cards")
    assert "Card body for block-composer" in text


def test_filter_missing_slug_returns_empty(fake_index):
    result = query.filter_concepts(fake_index, slug="does-not-exist")
    assert result == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/wiki/test_query.py -v`
Expected: failures — `filter_concepts` and `format_output` not defined.

- [ ] **Step 3: Replace `scripts/wiki/query.py` with new implementation**

Overwrite `scripts/wiki/query.py` entirely:

```python
"""Pure-Python filter over wiki/index.yaml.

CLI:
    python -m scripts.wiki.query --stage=08 --type=agent
    python -m scripts.wiki.query --tag=wordpress
    python -m scripts.wiki.query --slug=block-composer
    python -m scripts.wiki.query --trigger=landing-build
    python -m scripts.wiki.query --grep=gutenberg
    python -m scripts.wiki.query --slug=X --format=cards

Formats: compact (default), cards, slugs, json.
No SDK calls. <100ms for any query.
"""
from __future__ import annotations

import argparse
import json
import sys
from pathlib import Path
from typing import Any

import yaml

from scripts.wiki import config


def _load_index(wiki_dir: Path) -> dict[str, Any]:
    index_yaml = wiki_dir / "index.yaml"
    if not index_yaml.exists():
        return {"version": 1, "concepts": []}
    return yaml.safe_load(index_yaml.read_text(encoding="utf-8")) or {}


def filter_concepts(
    wiki_dir: Path,
    *,
    stage: str | None = None,
    type_: str | None = None,
    tag: str | None = None,
    trigger: str | None = None,
    slug: str | None = None,
    grep: str | None = None,
) -> list[dict[str, Any]]:
    """Возвращает список концептов из index.yaml, удовлетворяющих фильтрам."""
    data = _load_index(wiki_dir)
    result: list[dict[str, Any]] = []
    for c in data.get("concepts", []):
        if slug is not None and c.get("slug") != slug:
            continue
        if stage is not None and c.get("stage") != stage:
            continue
        if type_ is not None and c.get("type") != type_:
            continue
        if tag is not None and tag not in (c.get("tags") or []):
            continue
        if trigger is not None and trigger not in (c.get("triggers") or []):
            continue
        if grep is not None:
            haystack = " ".join(
                str(c.get(k, "")) for k in ("slug", "name", "tags", "triggers")
            ).lower()
            if grep.lower() not in haystack:
                continue
        result.append(c)
    return result


def _format_compact(concepts: list[dict[str, Any]]) -> str:
    lines: list[str] = []
    for c in concepts:
        tags = ",".join(c.get("tags") or [])
        triggers = ",".join(c.get("triggers") or [])
        head = f"[[{c.get('slug', '?')}]]"
        if tags:
            head += f" | tags: {tags}"
        if triggers:
            head += f" | triggers: {triggers}"
        lines.append(head)
        name = c.get("name", c.get("slug", ""))
        if name and name != c.get("slug"):
            lines.append(f"  {name}")
        if c.get("card"):
            lines.append(f"  card: {c['card']}")
        lines.append("")
    return "\n".join(lines).rstrip() + "\n"


def _format_slugs(concepts: list[dict[str, Any]]) -> str:
    return "\n".join(c.get("slug", "") for c in concepts) + "\n"


def _format_json(concepts: list[dict[str, Any]]) -> str:
    return json.dumps(concepts, ensure_ascii=False, indent=2)


def _format_cards(wiki_dir: Path, concepts: list[dict[str, Any]]) -> str:
    chunks: list[str] = []
    for c in concepts:
        card_rel = c.get("card")
        if not card_rel:
            continue
        card_path = wiki_dir / card_rel
        if card_path.exists():
            chunks.append(card_path.read_text(encoding="utf-8"))
    return "\n\n---\n\n".join(chunks) + "\n"


def format_output(
    wiki_dir: Path, concepts: list[dict[str, Any]], fmt: str = "compact"
) -> str:
    if fmt == "compact":
        return _format_compact(concepts)
    if fmt == "slugs":
        return _format_slugs(concepts)
    if fmt == "json":
        return _format_json(concepts)
    if fmt == "cards":
        return _format_cards(wiki_dir, concepts)
    raise ValueError(f"Unknown format: {fmt}")


def main() -> int:
    parser = argparse.ArgumentParser(
        description="Query wiki/index.yaml — pure-Python filter, no SDK."
    )
    parser.add_argument("--wiki", default=str(config.WIKI_DIR), help="wiki/ dir")
    parser.add_argument("--stage", help="Filter by stage (e.g. 08)")
    parser.add_argument("--type", dest="type_", help="Filter by type")
    parser.add_argument("--tag", help="Filter by tag")
    parser.add_argument("--trigger", help="Filter by trigger")
    parser.add_argument("--slug", help="Filter by exact slug")
    parser.add_argument("--grep", help="Substring search across slug/name/tags/triggers")
    parser.add_argument(
        "--format", dest="fmt", default="compact",
        choices=("compact", "cards", "slugs", "json"),
    )
    args = parser.parse_args()
    wiki_dir = Path(args.wiki).resolve()

    concepts = filter_concepts(
        wiki_dir,
        stage=args.stage,
        type_=args.type_,
        tag=args.tag,
        trigger=args.trigger,
        slug=args.slug,
        grep=args.grep,
    )
    sys.stdout.write(format_output(wiki_dir, concepts, fmt=args.fmt))
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/wiki/test_query.py -v`
Expected: 12 passed.

- [ ] **Step 5: Smoke test CLI**

Run: `python -m scripts.wiki.query --help`
Expected: argparse help printed without exception.

- [ ] **Step 6: Commit**

```bash
git add scripts/wiki/query.py tests/wiki/test_query.py
git commit -m "feat(wiki): rewrite query.py as pure-Python YAML filter (no SDK)"
```

---

## Stage 3 — Scope Narrowing & Prompt Update

### Task 6: Split SYSTEM_SOURCES into CORE_SOURCES and REFERENCE_SOURCES

**Files:**
- Modify: `scripts/wiki/config.py`
- Modify: `scripts/wiki/compile.py` (if it references SYSTEM_SOURCES)
- Test: extend `tests/wiki/test_config.py` (create if missing)

**Rationale:** Narrow what `compile --source-mode=system` walks: only the 5 core categories + `block-library/README.md` catalog. Everything else moves to `REFERENCE_SOURCES` (unused in this PR, kept for future).

- [ ] **Step 1: Find existing references to SYSTEM_SOURCES**

Run: `grep -rn "SYSTEM_SOURCES" scripts/ tests/`
Note all callsites — they will need to import `CORE_SOURCES` instead.

- [ ] **Step 2: Write the failing test**

Create or extend `tests/wiki/test_config.py`:

```python
"""Tests for scripts.wiki.config — source taxonomy."""
from scripts.wiki import config


def test_core_sources_exist():
    assert hasattr(config, "CORE_SOURCES")
    assert isinstance(config.CORE_SOURCES, list)
    assert len(config.CORE_SOURCES) >= 5


def test_core_sources_include_5_categories_plus_catalog():
    paths = {s["path"] for s in config.CORE_SOURCES}
    assert "agents/*.md" in paths
    assert "skills/*/SKILL.md" in paths
    assert "commands/*.md" in paths
    assert "template/*/README.md" in paths
    assert "docs/standards/*.md" in paths
    assert "block-library/README.md" in paths


def test_reference_sources_exist_but_not_used_by_default():
    """REFERENCE_SOURCES определён для будущего, но не входит в CORE."""
    assert hasattr(config, "REFERENCE_SOURCES")
    core_paths = {s["path"] for s in config.CORE_SOURCES}
    ref_paths = {s["path"] for s in config.REFERENCE_SOURCES}
    assert core_paths.isdisjoint(ref_paths)


def test_system_sources_aliased_to_core_for_back_compat():
    """SYSTEM_SOURCES alias = CORE_SOURCES — старый код не ломается."""
    assert config.SYSTEM_SOURCES is config.CORE_SOURCES
```

- [ ] **Step 3: Run test to verify it fails**

Run: `python -m pytest tests/wiki/test_config.py -v`
Expected: `AttributeError: module 'scripts.wiki.config' has no attribute 'CORE_SOURCES'`.

- [ ] **Step 4: Rewrite `scripts/wiki/config.py`**

Replace the `SYSTEM_SOURCES = [...]` block in `scripts/wiki/config.py` with:

```python
# Core sources — попадают в index.yaml, используются orchestrator для routing.
CORE_SOURCES = [
    {"path": "agents/*.md", "concept_dir": "agents"},
    {"path": "skills/*/SKILL.md", "concept_dir": "skills"},
    {"path": "commands/*.md", "concept_dir": "commands"},
    {"path": "template/*/README.md", "concept_dir": "stages"},
    {"path": "docs/standards/*.md", "concept_dir": "rules"},
    {"path": "block-library/README.md", "concept_dir": "catalogs"},
]

# Reference sources — справочный материал, в index.yaml НЕ попадает.
# Оставлено для будущего reference-compiler; в текущем PR не используется.
REFERENCE_SOURCES = [
    {"path": "block-library/*/*/meta.yaml", "concept_dir": "blocks"},
    {"path": "block-library/_patterns/*/meta.yaml", "concept_dir": "patterns"},
    {"path": "block-library/_styles/*/README.md", "concept_dir": "styles"},
    {"path": "config/*.yaml", "concept_dir": "config"},
    {"path": "docs/SETUP.md", "concept_dir": "docs"},
    {"path": "docs/superpowers/specs/*.md", "concept_dir": "specs"},
    {"path": "docs/superpowers/plans/*.md", "concept_dir": "plans"},
    {"path": "docs/ПЛАН-ДОРАБОТОК.md", "concept_dir": "docs"},
    {"path": "docs/BACKLOG.md", "concept_dir": "docs"},
    {"path": "docs/photo-selection-guide.md", "concept_dir": "docs"},
    {"path": "docs/superpowers/DOKRUTKA-system.md", "concept_dir": "docs"},
    {"path": "docs/superpowers/dokrutka*.md", "concept_dir": "docs"},
    {"path": "tests/*/README.md", "concept_dir": "tests"},
    {"path": "presets/*.md", "concept_dir": "presets"},
    {"path": "presets/*.yaml", "concept_dir": "presets"},
    {"path": "scripts/*.doc.md", "concept_dir": "scripts"},
    {"path": "scripts/*/*.doc.md", "concept_dir": "scripts"},
    {"path": "scripts/*/*/*.doc.md", "concept_dir": "scripts"},
]

# Back-compat alias — старый код, импортирующий SYSTEM_SOURCES, продолжает работать.
SYSTEM_SOURCES = CORE_SOURCES
```

- [ ] **Step 5: Run config tests**

Run: `python -m pytest tests/wiki/test_config.py -v`
Expected: 4 passed.

- [ ] **Step 6: Run full wiki test suite to confirm no regression**

Run: `python -m pytest tests/wiki/ -v`
Expected: all green.

- [ ] **Step 7: Commit**

```bash
git add scripts/wiki/config.py tests/wiki/test_config.py
git commit -m "feat(wiki): split SOURCES into CORE (in index) + REFERENCE (not in index)"
```

---

### Task 7: Update `prompts/system_concept.md` for new card contract

**Files:**
- Modify: `scripts/wiki/prompts/system_concept.md`

**Rationale:** Cards must now follow fixed 5/6-section body + carry full frontmatter (`triggers`, `gates`, `pre_reqs`, `inputs`, `outputs`, `related`). SDK auto-fills missing fields with `confidence: low` marker. No human editing required after generation.

- [ ] **Step 1: Read current prompt**

Already read (Step 4 above). Replace content entirely.

- [ ] **Step 2: Overwrite `scripts/wiki/prompts/system_concept.md`**

```markdown
Ты компилируешь knowledge wiki системы landing-system. На вход — markdown/yaml исходник одного компонента системы (агент, скилл, команда, этап шаблона, правило или каталог). На выход — структурированная wiki-карточка на русском языке.

# Формат ответа

Вернуть только содержимое markdown-файла с frontmatter, БЕЗ обрамляющих ```markdown```.

## Frontmatter (обязателен полный)

```
---
slug: kebab-case-name
type: agent | skill | command | stage | rule | catalog
name: "Человеческое имя"
stage: "08"                  # только для type=stage/agent/command, если известен этап
tags: [tag1, tag2]
triggers: [trigger-1]        # обязательно для agent/command/skill (может быть [])
inputs: [path/or/slug]       # обязательно для stage; для остальных опционально
outputs: [path/or/slug]      # обязательно для stage
gates: [gate-name]           # только для stage
pre_reqs: [slug-1]           # slug'и концептов, которые должны быть done раньше
related: [slug-1, slug-2]    # slug'и связанных концептов
sources: ["относительный/путь.md"]
updated: 2026-05-26
confidence: {field: low}     # ТОЛЬКО для полей, где ты не уверен
---
```

## Тело карточки (фиксированный порядок секций)

Для всех типов кроме stage — 5 секций:

```
# Заголовок (читаемое название)

## Что делает
Один абзац, 3-5 строк, в проектных терминах.

## Когда вызывается
Триггер + условие. Без жаргона.

## Вход → выход
**Вход:** что нужно на старте.
**Выход:** что появится после.

## Failure modes
3-5 буллетов про типичные поломки.

## Related
- [[slug-1]] — кратко зачем связан
- [[slug-2]] — кратко
```

Для type=stage добавляется **секция «Чем закрывается этап (gates)»** между «Вход → выход» и «Failure modes»:

```
## Чем закрывается этап (gates)
- gate-1 — что проверяется
- gate-2 — что проверяется
```

# Правила auto-fill

- **Заполняй ВСЕ поля frontmatter** для соответствующего типа. Если в исходнике данных нет — выдавай best-effort значение и помечай в `confidence`.
- Пример: исходник агента не указывает явно `triggers`. Ты выводишь из контекста (если агент относится к этапу 08, триггеры — `landing-build` или `approve-07b`) и помечаешь `confidence: {triggers: low}`.
- НЕ выдумывай slug'и в `related`/`pre_reqs`/`triggers` из воздуха. Используй только те, что упомянуты в исходнике или его контексте. Если уверенности нет — оставляй список пустым `[]` (это валидно).
- Список known slug'ов передаётся в user-prompt — сверяйся с ним.

# Ограничения

- 200-400 слов в теле.
- Простой русский язык.
- Все имена slug'ов в kebab-case.
- НЕ добавляй секций кроме указанных.
- НЕ выдавай примеры кода без необходимости.
```

- [ ] **Step 3: Commit (no test needed — prompt is data, validated by Stage 4 regen)**

```bash
git add scripts/wiki/prompts/system_concept.md
git commit -m "feat(wiki): new card contract prompt (fixed sections + auto-fill)"
```

---

### Task 8: Pass known slugs into SDK user prompt

**Files:**
- Modify: `scripts/wiki/system_compiler.py` (the `_compile_concept` function)
- Test: extend `tests/wiki/test_system_compiler.py`

**Rationale:** The prompt says "use only known slugs in related/pre_reqs". For that to work, the user-prompt sent to SDK must **include** the list of known slugs. Otherwise the model hallucinates names.

- [ ] **Step 1: Write the failing test**

Append to `tests/wiki/test_system_compiler.py`:

```python
def test_compile_concept_includes_known_slugs_in_user_prompt(fake_repo, tmp_path, mocker):
    """SDK получает список known slug'ов в user-prompt — модель не выдумывает refs."""
    wiki = tmp_path / "wiki"
    # Pre-create a concept so it's a known slug
    (wiki / "concepts" / "agents").mkdir(parents=True)
    (wiki / "concepts" / "agents" / "existing-agent.md").write_text(
        "---\nslug: existing-agent\ntype: agent\nname: existing-agent\n---\nbody",
        encoding="utf-8",
    )

    captured = {}
    def fake_generate(system, user):
        captured["user"] = user
        return "---\nslug: sample-agent\ntype: agent\nname: sample-agent\n---\nbody"

    mocker.patch("scripts.wiki.system_compiler.sdk_client.generate", side_effect=fake_generate)
    mocker.patch("scripts.wiki.system_compiler._build_index", return_value="idx")

    sources = [{"path": "agents/*.md", "concept_dir": "agents"}]
    system_compiler.compile_system(repo_root=fake_repo, wiki_dir=wiki, sources=sources)

    assert "existing-agent" in captured["user"]
    assert "known slugs" in captured["user"].lower() or "known_slugs" in captured["user"].lower()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/wiki/test_system_compiler.py::test_compile_concept_includes_known_slugs_in_user_prompt -v`
Expected: FAIL (no "known slugs" string in current user prompt).

- [ ] **Step 3: Modify `_compile_concept` to accept and inject known slugs**

In `scripts/wiki/system_compiler.py`, change `_compile_concept` signature:

```python
def _compile_concept(
    source_path: Path,
    repo_root: Path,
    known_slugs: list[str] | None = None,
) -> str:
    """Зовёт SDK для одного исходника, возвращает markdown концепта."""
    system_prompt = _load_prompt("system_concept.md")
    rel = source_path.relative_to(repo_root).as_posix()
    slugs_block = ""
    if known_slugs:
        slugs_list = ", ".join(sorted(set(known_slugs)))
        slugs_block = f"\n\n## Known slugs (для related/pre_reqs)\n{slugs_list}\n"
    user_msg = (
        f"Источник: `{rel}`\n\n---\n\n"
        f"{source_path.read_text(encoding='utf-8')}"
        f"{slugs_block}"
    )
    return sdk_client.generate(system=system_prompt, user=user_msg)
```

- [ ] **Step 4: Pass known_slugs from `compile_system`**

Inside `compile_system`, before the main loop, collect existing slugs from `wiki/concepts/`:

```python
    existing_slugs: list[str] = []
    concepts_root = wiki_dir / "concepts"
    if concepts_root.exists():
        existing_slugs = [p.stem for p in concepts_root.rglob("*.md")]
```

Then in the loop where `_compile_concept` is called, pass it:

```python
                content = _compile_concept(source_path, repo_root, known_slugs=existing_slugs)
```

- [ ] **Step 5: Run test to verify it passes**

Run: `python -m pytest tests/wiki/test_system_compiler.py -v`
Expected: all green including the new test.

- [ ] **Step 6: Commit**

```bash
git add scripts/wiki/system_compiler.py tests/wiki/test_system_compiler.py
git commit -m "feat(wiki): pass known slugs to SDK to prevent hallucinated refs"
```

---

### Task 9: Delete obsolete reference concept directories

**Files:**
- Delete: `wiki/concepts/{blocks,patterns,plans,specs,scripts,tests,config,docs,presets,styles}/`
- Modify: `wiki/.cache.json` (entries naturally drop on next compile; no manual edit)

**Rationale:** REFERENCE_SOURCES no longer compiled into wiki. Their concept dirs are stale. Removal is safe — these were auto-generated, nothing in `agents/`, `commands/`, etc. depends on them.

- [ ] **Step 1: List directories about to be deleted**

Run: `ls wiki/concepts/`
Expected: 14 dirs. Confirm which fall into the delete set (anything except `agents`, `skills`, `commands`, `stages`, `rules`, `catalogs`).

- [ ] **Step 2: Delete reference concept dirs**

Run from `d:/AI_TEAMS/landing_system`:

```bash
rm -rf wiki/concepts/blocks wiki/concepts/patterns wiki/concepts/plans wiki/concepts/specs wiki/concepts/scripts wiki/concepts/tests wiki/concepts/config wiki/concepts/docs wiki/concepts/presets wiki/concepts/styles
```

(If any of these dirs don't exist, `rm -rf` ignores silently.)

- [ ] **Step 3: Verify what remains**

Run: `ls wiki/concepts/`
Expected: only `agents/`, `skills/`, `commands/`, `stages/`, `rules/` (and possibly `catalogs/` once compile creates it).

- [ ] **Step 4: Commit deletions**

```bash
git add -u wiki/concepts/
git commit -m "chore(wiki): remove reference concept dirs (moved to REFERENCE_SOURCES, not compiled)"
```

---

### Task 10: Delete CORE concept files to force regeneration in new format

**Files:**
- Delete: `wiki/concepts/{agents,skills,commands,stages,rules}/*.md`

**Rationale:** Stage 4 regenerates everything in the new contract. Old cards have the old body format — easier to delete + regen than migrate. Cache invalidation happens because `system_compiler._populate_cache` only adds keys for cards that **exist**; deleting the card removes its cache entry on next compile.

- [ ] **Step 1: Confirm what gets deleted**

Run: `find wiki/concepts/agents wiki/concepts/skills wiki/concepts/commands wiki/concepts/stages wiki/concepts/rules -name "*.md" | wc -l`
Expected: ~100 files.

- [ ] **Step 2: Delete CORE cards (keep directories)**

```bash
find wiki/concepts/agents -name "*.md" -delete
find wiki/concepts/skills -name "*.md" -delete
find wiki/concepts/commands -name "*.md" -delete
find wiki/concepts/stages -name "*.md" -delete
find wiki/concepts/rules -name "*.md" -delete
```

- [ ] **Step 3: Reset the cache entries for these sources**

Open `wiki/.cache.json` and remove entries whose keys match the core-source globs. Simpler: delete the whole cache file — compile will rebuild it.

```bash
rm wiki/.cache.json
```

- [ ] **Step 4: Commit pre-regen state**

```bash
git add -u wiki/concepts/ wiki/.cache.json
git commit -m "chore(wiki): clear CORE cards + cache for regen in new contract"
```

---

## Stage 4 — Regenerate Cards in New Contract

### Task 11: First regeneration pass with throttle

**Files:**
- Modify: `wiki/index.yaml` (auto-generated)
- Modify: `wiki/concepts/{agents,skills,commands,stages,rules,catalogs}/*.md` (auto-generated)

**Rationale:** Run compile under throttle. Generates up to 20 cards per run + writes `index.yaml`. With ~100 CORE sources, expect ~5 runs.

- [ ] **Step 1: First compile run**

Run: `python -m scripts.wiki.compile --source-mode=system`
Expected output ends with something like: `compiled 20 ... deferred 80 ... ⚠️ 80 source(s) deferred — re-run compile to finish them (WIKI_MAX_SDK_CALLS=20).`

If SDK quota is exhausted, you'll see `SDK error: ...` lines and the run still emits `index.yaml` with `incomplete: true` markers. That's expected — stop here and wait for quota to refresh, then continue.

- [ ] **Step 2: Verify index.yaml emerged**

Run: `ls -la wiki/index.yaml && head -20 wiki/index.yaml`
Expected: file exists, contains `version: 1` and `concepts:` list.

- [ ] **Step 3: Commit first batch**

```bash
git add wiki/concepts/ wiki/index.yaml wiki/index.md wiki/log.md wiki/.cache.json
git commit -m "chore(wiki): regenerate cards batch 1/N (throttle=20)"
```

- [ ] **Step 4: Repeat until no deferred**

Loop:
1. `python -m scripts.wiki.compile --source-mode=system`
2. If output says "deferred N (N>0)", commit `chore(wiki): regenerate cards batch K/N` and repeat.
3. If output says no deferred — done.

Expected: 4-6 iterations total.

- [ ] **Step 5: Final sanity check on YAML index**

Run: `python -c "import yaml; d=yaml.safe_load(open('wiki/index.yaml',encoding='utf-8')); print('total:', d['counts']['total']); print('types:', d['counts']['by_type'])"`
Expected: ~100 total, distributed across stage/agent/skill/command/rule/catalog.

- [ ] **Step 6: Run lint, accept low_confidence warnings**

Run: `python -m scripts.wiki.lint --wiki wiki`
Expected: exit code 0 (or 1 with only broken_refs that are real bugs — fix them by re-running compile for those specific sources after correcting the source file).

If broken_refs appears: read offending source file, ensure related slug exists (or fix the source's frontmatter), delete that card, re-run compile.

---

## Stage 5 — Hook & Orchestrator Wiring

### Task 12: Replace SessionStart hook injection with 50-token hint

**Files:**
- Modify: `scripts/wiki/hooks/session_start.py`
- Test: `tests/wiki/test_hooks.py` (extend or create)

**Rationale:** Current hook injects full `wiki/index.md` (~3K tokens) on every session. New hook injects a tiny hint with concept count + query command (~50 tokens).

- [ ] **Step 1: Read current hook (already done above)**

The hook reads `wiki/index.md` and `<project>/wiki/index.md` plus recent memory.

- [ ] **Step 2: Write the failing test**

Create or extend `tests/wiki/test_hooks.py`:

```python
"""Tests for SessionStart hook — minimal wiki hint."""
import json
import subprocess
import sys
from pathlib import Path

import yaml
import pytest

HOOK = Path(__file__).resolve().parents[2] / "scripts" / "wiki" / "hooks" / "session_start.py"


def _run_hook(cwd: str, env_overrides: dict | None = None) -> str:
    """Invoke hook as subprocess with given cwd in stdin payload."""
    import os
    env = os.environ.copy()
    if env_overrides:
        env.update(env_overrides)
    proc = subprocess.run(
        [sys.executable, str(HOOK)],
        input=json.dumps({"cwd": cwd}),
        capture_output=True,
        text=True,
        env=env,
    )
    return proc.stdout


def test_hook_emits_wiki_hint_not_full_index(tmp_path, monkeypatch):
    """Hook должен печатать <wiki_runtime> hint вместо полного index.md."""
    landing_system = Path(__file__).resolve().parents[2]
    wiki_dir = landing_system / "wiki"
    # Sanity: index.yaml exists (from Stage 4)
    if not (wiki_dir / "index.yaml").exists():
        pytest.skip("wiki/index.yaml not yet generated (run Stage 4 first)")

    output = _run_hook(cwd=str(landing_system))
    assert "<wiki_runtime>" in output
    assert "wiki/index.yaml" in output
    assert "python -m scripts.wiki.query" in output
    # The big system_wiki_index block must NOT appear
    assert "<system_wiki_index>" not in output


def test_hook_returns_empty_when_no_yaml(tmp_path):
    """Если index.yaml нет — hint не выдаётся, hook молчит."""
    output = _run_hook(cwd=str(tmp_path))
    assert "<wiki_runtime>" not in output
```

- [ ] **Step 3: Run test to verify it fails**

Run: `python -m pytest tests/wiki/test_hooks.py -v`
Expected: FAIL because current hook still emits `<system_wiki_index>`.

- [ ] **Step 4: Rewrite `scripts/wiki/hooks/session_start.py`**

Replace the whole file:

```python
#!/usr/bin/env python3
"""SessionStart hook: печатает компактный wiki hint (~50 tokens).

Old behavior (deprecated): инжектил полный wiki/index.md (~3K tokens) на каждой
сессии. New behavior: печатает только пойнтер на wiki/index.yaml + команду
запроса. Orchestrator решает САМ, когда подгружать карточки.

Сохраняет проектный wiki (~/Lendings/<slug>/wiki/index.md) и recent memory —
они контекстные и компактные.
"""
from __future__ import annotations

import json
import sys
from pathlib import Path

LANDING_SYSTEM = Path(__file__).resolve().parents[3]


def _read_or_empty(p: Path, max_chars: int = 8000) -> str:
    if not p.exists():
        return ""
    try:
        text = p.read_text(encoding="utf-8")
        if len(text) > max_chars:
            text = text[:max_chars] + "\n\n[...обрезано]"
        return text
    except OSError:
        return ""


def _detect_project_slug(cwd: Path) -> str | None:
    from scripts.lib.paths import LANDINGS_ROOT
    try:
        rel = cwd.resolve().relative_to(LANDINGS_ROOT)
    except ValueError:
        return None
    parts = rel.parts
    return parts[0] if parts else None


def _latest_daily(memory_dir: Path) -> str:
    daily = memory_dir / "daily"
    if not daily.exists():
        return ""
    files = sorted(daily.glob("*.md"))
    if not files:
        return ""
    return _read_or_empty(files[-1], max_chars=4000)


def _system_wiki_hint() -> str:
    """50-token hint про существование системной wiki + команду запроса."""
    index_yaml = LANDING_SYSTEM / "wiki" / "index.yaml"
    if not index_yaml.exists():
        return ""
    try:
        import yaml
        data = yaml.safe_load(index_yaml.read_text(encoding="utf-8")) or {}
    except Exception:
        return ""
    total = (data.get("counts") or {}).get("total", "?")
    return (
        "<wiki_runtime>\n"
        f"Landing-system wiki: {total} concepts indexed at wiki/index.yaml.\n"
        "Query: python -m scripts.wiki.query --stage=N --type=T --tag=X --slug=Y\n"
        "Read card: cat wiki/concepts/<dir>/<slug>.md\n"
        "</wiki_runtime>"
    )


def main() -> int:
    try:
        payload = json.load(sys.stdin)
    except (json.JSONDecodeError, ValueError):
        payload = {}

    cwd_str = payload.get("cwd") or str(Path.cwd())
    cwd = Path(cwd_str)

    chunks: list[str] = []

    # System wiki hint — only if cwd is inside landing-system tree
    try:
        cwd.resolve().relative_to(LANDING_SYSTEM)
        hint = _system_wiki_hint()
        if hint:
            chunks.append(hint)
    except ValueError:
        pass

    # Project wiki — full inject (small per-project file, kept as-is)
    slug = _detect_project_slug(cwd)
    if slug:
        from scripts.lib.paths import project_dir
        project = project_dir(slug)
        proj_index = _read_or_empty(project / "wiki" / "index.md")
        if proj_index:
            chunks.append(
                f"<project_wiki_index project=\"{slug}\">\n{proj_index}\n</project_wiki_index>"
            )
        memory_recent = _latest_daily(project / "memory")
        if memory_recent:
            chunks.append(
                f"<project_recent_memory project=\"{slug}\">\n{memory_recent}\n</project_recent_memory>"
            )

    if chunks:
        print("\n\n".join(chunks))

    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 5: Run test to verify it passes**

Run: `python -m pytest tests/wiki/test_hooks.py -v`
Expected: 2 passed (or 1 passed + 1 skipped if Stage 4 not finished).

- [ ] **Step 6: Commit**

```bash
git add scripts/wiki/hooks/session_start.py tests/wiki/test_hooks.py
git commit -m "feat(wiki): replace SessionStart full-index inject with 50-token hint"
```

---

### Task 13: Add "Wiki as roadmap" section to landing-orchestrator

**Files:**
- Modify: `agents/landing-orchestrator.md`

**Rationale:** Tell the orchestrator agent (system-prompted) that wiki exists, how to query it, and that querying happens **before** loading a source file. This is the behavioral change that actually saves tokens.

- [ ] **Step 1: Find current content**

Run: `grep -n "^#" agents/landing-orchestrator.md | head -20`
Note the section structure to choose where to insert the new section (suggest after intro, before tool list).

- [ ] **Step 2: Read current file to find insertion point**

Run: `head -60 agents/landing-orchestrator.md`
Identify a header right after the agent's role description.

- [ ] **Step 3: Insert "Wiki как roadmap" section**

Use Edit to add the following block immediately after the role/intro section (before the first stage-specific section):

```markdown
## Wiki как roadmap

Перед запуском каждого этапа сначала запроси системную wiki, чтобы получить кандидатов:

```bash
python -m scripts.wiki.query --stage=<N> --type=agent
python -m scripts.wiki.query --slug=<concept-slug>
```

Прочитай вывод (3-5 карточек) и выбери одного агента/скилл. **Только** после этого подгружай полный исходник из `agents/<slug>.md`, если карточка не покрывает вопрос.

Это экономит ~90% токенов на routing-решениях.

Wiki sources of truth:
- `wiki/index.yaml` — машинный индекс (auto-generated, не правится руками)
- `wiki/concepts/<dir>/<slug>.md` — карточки с frontmatter (контракт этапа/агента)

Если карточки нет — это значит, что compile не успел её сгенерить (throttle). Открой исходник напрямую.
```

- [ ] **Step 4: Smoke verify**

Run: `grep -c "Wiki как roadmap" agents/landing-orchestrator.md`
Expected: `1`.

Run: `grep -c "scripts.wiki.query" agents/landing-orchestrator.md`
Expected: at least `1`.

- [ ] **Step 5: Commit**

```bash
git add agents/landing-orchestrator.md
git commit -m "feat(orchestrator): wiki-first routing — query index before loading sources"
```

---

### Task 14: Final integration smoke

**Files:** none modified (verification only).

- [ ] **Step 1: Run full wiki test suite**

Run: `python -m pytest tests/wiki/ -v`
Expected: all green.

- [ ] **Step 2: Run query CLI end-to-end**

Run: `python -m scripts.wiki.query --stage=08 --type=agent`
Expected: list of agents bound to stage 08 (e.g., block-composer, frontend-builder).

Run: `python -m scripts.wiki.query --slug=landing-orchestrator --format=cards`
Expected: full card body printed.

Run: `python -m scripts.wiki.query --grep=gutenberg`
Expected: at least one match.

- [ ] **Step 3: Verify SessionStart hook emits hint**

Run: `echo '{"cwd": "d:/AI_TEAMS/landing_system"}' | python scripts/wiki/hooks/session_start.py`
Expected: stdout contains `<wiki_runtime>` block with count + query command. Does NOT contain `<system_wiki_index>`.

- [ ] **Step 4: Run lint**

Run: `python -m scripts.wiki.lint --wiki wiki`
Expected: exit 0, or exit 1 only if there are genuine broken_refs/dup_slugs (no `low_confidence`/`stale`/`empty` should fail).

- [ ] **Step 5: Final commit (optional — if any cleanup arose)**

Only commit if files actually changed during smoke.

```bash
git status --short
# if anything modified by smoke runs (e.g., wiki/log.md from compile):
git add wiki/
git commit -m "chore(wiki): post-integration log/cache updates"
```

---

## Done Criteria

After all 14 tasks complete:

- `wiki/index.yaml` exists, contains ~100 concepts across 6 types.
- `python -m scripts.wiki.query --stage=08` returns filtered results in <200ms.
- SessionStart hook emits `<wiki_runtime>` block, not `<system_wiki_index>`.
- `agents/landing-orchestrator.md` contains "Wiki как roadmap" section.
- All tests in `tests/wiki/` are green.
- Stale reference concept dirs deleted; only CORE category dirs remain in `wiki/concepts/`.

## Rollback

Every stage is one or more commits. Roll back by `git revert <hash>`:

- Stage 5: revert Tasks 12-13 → hook and orchestrator back to old behavior; index.yaml stays but unused.
- Stage 4: revert Task 11 batch commits → cards back to pre-regen state (empty).
- Stage 3: revert Tasks 6-10 → SYSTEM_SOURCES restored, all reference dirs re-added via re-compile.
- Stage 2: revert Task 5 → old SDK-based query.py restored.
- Stage 1: revert Tasks 1-4 → no YAML index, no new lint rules; system_compiler.py back to original.

No data loss — everything is in git history.
