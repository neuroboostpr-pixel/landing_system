# Landing-System Wiki — главный индекс

> Авто-сгенерированный индекс. Обновляется при `python -m scripts.wiki.compile --source-mode=system`.

**Концептов всего:** 102
**Категории:** agent, command, rule, skill, stage

**Известные ограничения:**
- Блоки из `block-library/` ещё не в wiki — требует отдельной задачи (структура не подходит под текущий glob).
- Часть `wikilinks` ссылается на скрипты/конфиги вне wiki — это норма (`gate-check.sh`, `.landing-state.yaml` и т.д.).

## 📋 Этапы pipeline

- [[00-brif]] — Хранит `brief.md` — короткое описание проекта: ниша, целевая аудитория, KPI. Это стартовая точка информации о проекте для всех последующих э
- [[01-kontekst]] — Хранит `context.md` — текстовый файл с деталями клиента, описанием целевой аудитории и дополнительными требованиями к лендингу. Это базовый 
- [[01a-analiz-nishi]] — Автоматически исследует рынок: определяет тип бренда, анализирует конкурентов, выбирает режим позиционирования (rational / emotional_aspirat
- [[02-materialy-klienta]] — Исторически служила точкой сбора всех клиентских материалов: фотографий, документов и прочих файлов. В современном prototype-first flow эта 
- [[03-referensy]] — Папка хранит список сайтов-образцов, которые нравятся клиенту или подходят по нише. На основе этих URL система строит мудборд и выбирает виз
- [[04-brend]] — Хранит все брендовые активы лендинга: логотипы клиента и автоматически сгенерированный бренд-кит с палитрой, шрифтами и иконографикой. Это «
- [[05-dizayn-sistema]] — Превращает бренд-кит в машиночитаемые дизайн-токены и единый источник истины о стиле лендинга: цвета, шрифты, отступы, визуальный preview
- [[06-stek]] — Фиксирует технологический стек лендинга: WordPress-плагины, JS-библиотеки, набор иконок и CDN шрифтов. Этот этап выполняется полностью автом
- [[07-kontent]] — Этап автоматического создания текстов лендинга: агент читает структурированный прототип и формирует готовый копирайт по каждому блоку страни
- [[07-prototip]] — Хранит прототип лендинга от клиента или маркетолога — основной документ с текстами, структурой и логикой страницы. Всё, что нужно для сборки
- [[07a-wireframe]] — На этом этапе система показывает маркетологу интерактивный HTML-превью будущего лендинга: для каждого блока страницы предлагается 2–3 вариан
- [[07b-composed]] — Собирает финальный HTML-макет лендинга: накладывает дизайн-систему (цвета, шрифты, отступы из `tokens.json`) и реальные тексты/CTA из протот
- [[07c-photos]] — Папка `07c_PHOTOS` — рабочее место для всех клиентских фотографий. Здесь фотки принимаются, автоматически сортируются по типу, расставляются
- [[07d-visuals]] — Генерирует PNG-иконки и инфографику для лендинга через codex (gpt-image-2), вставляет их в `composed.html` вместо текстовых плейсхолдеров. Б
- [[08-kod]] — Хранит готовый код WordPress-темы: PHP-шаблоны, CSS, JS, Gutenberg-блоки и конфиг полей ACF. Всё это генерируется автоматически агентом `wp-
- [[09-deploy]] — Папка хранит артефакты финального деплоя WordPress-лендинга на хостинг Бегет: лог последнего развёртывания и конфигурационный файл с SSH-дан
- [[10-qa]] — Автоматическая проверка живого сайта после деплоя: доступность, HTTPS, мета-теги, формы, аналитика, скорость загрузки и корректность отображ
- [[11-analitika]] — Папка хранит конфигурацию систем веб-аналитики для лендинга: настройки счётчиков (Яндекс.Метрика / Google Analytics) и список событий, котор
- [[12-seo]] — Финальный автоматический этап pipeline: агент `seo-optimizer` генерирует все необходимые SEO-артефакты для лендинга — мета-теги, OG-разметку

## 🤖 Агенты

- [[analytics-engineer]] — Подключает счётчик Яндекс.Метрики к WordPress-лендингу: вставляет код счётчика в тему, определяет цели (клики по CTA, отправки форм) и готов
- [[block-composer]] — Собирает `composed.html` — итоговый цветной макет лендинга с реальными текстами, CTA и дизайн-токенами. Фото, иконки и инфографика на этом э
- [[brand-architect]] — Берёт все извлечённые стилевые данные (цвета, шрифты, иконки, сетку, анимации) и собирает из них единый бренд-кит с полной «родословной» каж
- [[client-assets-collector]] — Собирает все исходные материалы клиента: фотографии, видео и отзывы с публичных площадок (Яндекс Карты, 2GIS, Otzovik, Flamp). Работает без 
- [[content-writer]] — Берёт черновой прототип текста лендинга и превращает его в готовый копирайт, разложенный по конкретным блокам страницы. Параллельно пишет SE
- [[design-system-generator]] — Берёт готовый бренд-кит и превращает его в полноценную дизайн-систему: файл с токенами, машиночитаемый JSON и живой HTML-превью с компонента
- [[frontend-builder]] — Берёт вайрфреймы из DESIGN.md §5 и превращает их в настоящий CSS-код (per-block правила) и рабочие PHP-шаблоны блоков Lazy Blocks. После нег
- [[icon-generator]] — Генерирует **один** файл иконки в формате PNG для конкретного слота лендинга. Работает через codex image_gen с умным кэшированием: если икон
- [[infographic-builder]] — Генерирует **один PNG-файл инфографики** для конкретного слота в лендинге через codex image_gen. Работает с числовыми, столбчатыми, линейным
- [[integrations-engineer]] — Настраивает формы захвата лидов и вебхуки: подключает Fluent Forms к Telegram-боту и/или CRM, чтобы каждая заявка с лендинга мгновенно уходи
- [[landing-onboarding-wizard]] — Ведёт маркетолога за руку от нуля до готовой папки проекта со всеми материалами. Объясняет систему, создаёт структуру директорий, помогает р
- [[landing-orchestrator]] — Ведёт лендинг-проект через все 12 этапов производства: от брифа до деплоя и SEO. На каждом этапе вызывает нужного специализированного агента
- [[lifecycle-keeper]] — Сохраняет снапшоты лендинга в конкретный момент времени, позволяет откатиться к любой предыдущей версии и создаёт полные A/B-клоны проекта н
- [[moodboard-composer]] — Берёт одобренные референсы из `03_РЕФЕРЕНСЫ/index.yaml` и собирает два артефакта: текстовое описание визуального направления (`moodboard.md`
- [[niche-analyst]] — Исследует нишу, конкурентов и рынок полностью в автоматическом режиме — без единого вопроса к пользователю. По итогам выдаёт 6 структурирова
- [[onboarding-guide]] — Проводит пользователя через первичную настройку landing-system: объясняет систему, проверяет зависимости, помогает заполнить API-ключи в `.e
- [[photo-classifier]] — Анализирует одну фотографию через codex CLI и создаёт структурированные метаданные: теги, подпись, кадрирование, совместимость с брендом. Са
- [[photo-curator]] — Управляет полным процессом работы с клиентскими фотографиями: принимает фотки из папки `inbox/`, классифицирует их через AI, подбирает лучши
- [[photo-matcher]] — Один раз анализирует каталог клиентских фотографий и автоматически подбирает топ-3 кандидата для каждого фото-слота в wireframe. Если подход
- [[photo-preview-board]] — Берёт утверждённый пользователем файл `selections.yaml` и превращает каждый слот в реальное изображение: обрезает и масштабирует клиентские 
- [[photo-stylist]] — Обрабатывает «сырые» фотографии клиента для использования в лендинге: вырезает фон, подчищает края, накладывает тени и текстуры. Главное огр
- [[prototype-importer]] — Принимает пользовательский прототип (PDF или Markdown) из папки `source/`, извлекает структуру, задаёт уточняющие вопросы при неясностях и с
- [[qa-auditor]] — Автоматически проверяет живой сайт по 7 критериям качества сразу после деплоя: от доступности и HTTPS до форм и аналитики. Формирует отчёт в
- [[references-curator]] — Собирает визуальные референсы (URL-сайтов, Behance/Dribbble, скриншоты), присваивает каждому статус и ведёт реестр в `03_РЕФЕРЕНСЫ/index.yam
- [[scene-director]] — Проектирует кинематографическую архитектуру лендинга: делит страницу на 6–8 сцен с описанием визуала, анимаций и параллакса. Результат — гот
- [[seo-optimizer]] — Подключает лендинг к поисковым системам: прописывает мета-теги, Open Graph, Schema.org и robots.txt на основе готового SEO-текста и данных б
- [[stack-planner]] — Выбирает и фиксирует набор WordPress-плагинов, JS-библиотек, иконок и шрифтов на основе готовой дизайн-системы. Результат — `design-stack.ya
- [[style-extractor]] — Анализирует одобренные референсы (картинки и URL) и вытаскивает из них конкретные стилевые параметры: цветовую палитру, шрифты, иконографику
- [[system-setup]] — Одноразовый агент, который подготавливает landing-system к работе: проверяет окружение, настраивает переменные среды и конфиг системы. После
- [[ux-composer]] — Превращает утверждённый прототип (`prototype.yaml`) в интерактивный `wireframe.html`: для каждого блока страницы предлагает 2–3 варианта ком
- [[visual-curator]] — Сканирует `composed.html` на наличие слотов для иконок и инфографики, запускает AI-генерацию через codex, кэширует результаты и вставляет го
- [[wp-builder]] — Генерирует готовый PHP-код WordPress-темы: Lazy Blocks для каждого блока лендинга, CSS с дизайн-токенами и JS-интеракции. На выходе — рабочи
- [[wp-deployer]] — Загружает готовую WordPress-тему на хостинг Бегет через SSH+rsync, активирует тему и импортирует ACF-поля. После деплоя проверяет доступност

## 🛠 Скиллы

- [[block-composition]] — Берёт утверждённые блоки из wireframe, дизайн-токены и тексты из прототипа — и собирает из них единый HTML-файл лендинга (`composed.html`). 
- [[block-library-management]] — Предоставляет утилиты для обслуживания общей библиотеки wireframe-блоков: создаёт новые блоки по шаблону, проверяет корректность каталога и 
- [[brand-kit-build]] — Собирает единый бренд-кит проекта из извлечённых стилевых данных: формирует файл `brand-kit.md` с полной прослеживаемостью каждого токена (ц
- [[client-assets-collection]] — Собирает все клиентские материалы на этапе 02: создаёт структуру папок в `02_МАТЕРИАЛЫ_КЛИЕНТА/`, парсит отзывы с внешних площадок (Яндекс.К
- [[design-tokens-generation]] — Читает готовый бренд-кит проекта и автоматически собирает полную дизайн-систему: файл `DESIGN.md` с описанием всех визуальных правил и машин
- [[landing-from-context]] — Создаёт новый лендинг-проект внутри системы, унаследовав данные из уже существующей папки агентства — аудиторию, исследования, прототип текс
- [[landing-onboarding]] — Настраивает landing-system на новой машине: проверяет локальные зависимости, MCP-серверы, плагин superpowers и все API-ключи. По завершении 
- [[landing-project-init]] — Создаёт чистую папку нового проекта лендинга по шаблону и запускает workflow с нулевого этапа. Это стартовая точка для проектов «с нуля» — н
- [[landing-versioning-and-cloning]] — Сохраняет снапшоты (версии) готового лендинга и позволяет откатиться к любой из них. Также создаёт полную копию проекта для A/B-тестирования
- [[moodboard-creation]] — Читает файл `03_РЕФЕРЕНСЫ/index.yaml`, разделяет референсы на одобренные и отклонённые, затем рендерит визуальный HTML-превью мудборда (`moo
- [[niche-analysis]] — Запускает полностью автоматический ресёрч ниши: ищет конкурентов, классифицирует тип бренда, определяет режим позиционирования и создаёт 6 г
- [[paralaximus-codex]] — Создаёт hero-блок с эффектом глубины: одна 2K-картинка нарезается на 4 слоя (фон, дальний план, ближний план, главный объект), фоны снимаютс
- [[photo-curation]] — Автоматически обрабатывает фотографии клиента: классифицирует их через AI, подбирает лучшие фото к каждому слоту макета, кадрирует и готовит
- [[photo-styling]] — Применяет технические трансформации к клиентским фотографиям: вырезает фон, сглаживает края, обрезает и масштабирует изображения. Все операц
- [[prototype-import]] — Принимает пользовательский прототип в формате PDF или Markdown, парсит его и преобразует в два нормализованных файла: читаемый `prototype.md
- [[references-collection]] — Ведёт YAML-файл `03_РЕФЕРЕНСЫ/index.yaml`, в котором хранятся все визуальные референсы проекта (ссылки и файлы) с присвоенными статусами: ка
- [[style-decomposition]] — Автоматически извлекает цвета, шрифты и иконки из картинок и веб-ссылок референсов. На выходе — пять структурированных файлов, которые потом
- [[visual-generation]] — Автоматически генерирует иконки и инфографику через codex image_gen и подставляет их в `composed.html` вместо текстовых плейсхолдеров. Бренд
- [[wireframe-rendering]] — Строит `wireframe.html` — интерактивную страницу-предпросмотр, где для каждого блока прототипа показаны 2–3 варианта композиции из библиотек
- [[wp-cli-deployer]] — Автоматически разворачивает готовую WordPress-тему на хостинге Бегет: синхронизирует файлы, активирует тему и плагины, импортирует изображен
- [[wp-gutenberg-block-builder]] — Берёт дизайн-токены, описание блоков и готовый текст — и генерирует полноценную WordPress-тему с Gutenberg-блоками на плагине **Lazy Blocks 
- [[wp-theme-assembler]] — Собирает готовую WordPress-тему: скачивает шрифты, SVG-иконки и обрабатывает фотографии в папку `assets/`, затем рендерит HTML-превью (`buil

## ⚡ Команды

- [[landing-brand]] — Собирает финальный бренд-кит проекта: берёт все извлечённые стили (палитра, шрифты, иконки) и синтезирует их в единый документ `brand-kit.md
- [[landing-build]] — Генерирует полноценную WordPress-тему с Lazy Blocks, подключает формы и CRM-интеграции, настраивает аналитику и SEO. На выходе — готовый к д
- [[landing-clone]] — Создаёт независимую копию существующего лендинга на новом поддомене — для A/B-тестирования или запуска параллельного варианта. Копия полност
- [[landing-compose]] — Собирает финальный HTML-файл лендинга с подставленными дизайн-токенами и текстами из прототипа. Визуальный контент (фото, иконки, инфографик
- [[landing-content]] — Берёт черновой прототип текста и структуру блоков из DESIGN.md, добавляет реальные отзывы клиента и выдаёт два готовых файла: финальный копи
- [[landing-deploy]] — Загружает готовую WordPress-тему на хостинг Бегет по SSH+rsync, активирует тему через WP-CLI и проверяет, что сайт живой. После деплоя польз
- [[landing-design]] — Команда запускает генерацию дизайн-системы проекта: читает утверждённый бренд-кит и превращает его в машиночитаемые токены (`tokens.json`), 
- [[landing-from-context]] — Создаёт новый проект лендинга и автоматически переносит в него данные из текущей папки агентства — аудиторию, исследования, прототип. Избавл
- [[landing-go]] — Единая точка входа для всего производства лендинга. Запускаешь один раз — команда сама читает статус проекта, находит следующий этап и ведёт
- [[landing-help]] — Выводит полный список всех slash-команд landing-system с кратким описанием каждой. Ничего не создаёт и не изменяет — только справка
- [[landing-moodboard]] — Создаёт визуальный мудборд лендинга из одобренных референсов: запускает агента `moodboard-composer`, который рендерит HTML-превью с настроен
- [[landing-new]] — Создаёт новый проект лендинга «с нуля»: разворачивает папку из шаблона, инициализирует файл состояния `.landing-state.yaml` и передаёт управ
- [[landing-niche]] — Запускает автоматический анализ рыночной ниши для текущего проекта-лендинга: изучает конкурентов, формирует профиль рынка и выбирает стратег
- [[landing-onboarding]] — Запускает интерактивный мастер настройки системы для нового пользователя: проверяет все зависимости, помогает ввести API-ключи и фиксирует з
- [[landing-photos]] — Запускает полный pipeline обработки клиентских фотографий: AI-классификация снимков, подбор лучших кандидатов под каждый слот лендинга, ручн
- [[landing-prototype]] — Берёт файл прототипа (`prototype.pdf` или `prototype.md`), который маркетолог положил в папку проекта, и преобразует его в два стандартных ф
- [[landing-qa]] — Проверяет опубликованный лендинг по 7 критериям качества: доступность сайта, HTTPS, мета-теги, аналитика, формы, мобильная версия и производ
- [[landing-references]] — Собирает и систематизирует визуальные ссылки-референсы (сайты, Behance, скриншоты), помечает каждый как `candidate / approved / rejected`, а
- [[landing-rollback]] — Восстанавливает WordPress-тему из ранее сохранённого снапшота и сразу же деплоит её на сервер. Позволяет быстро вернуться к рабочему состоян
- [[landing-setup]] — Настраивает всю инфраструктуру landing-system перед первым использованием: проверяет зависимости, собирает API-ключи, записывает конфигураци
- [[landing-stack]] — Определяет, какие WordPress-плагины, JS-библиотеки, иконочный набор и CDN-шрифты будут использованы в лендинге. Результат — YAML-реестр `des
- [[landing-start]] — Запускает интерактивный wizard, который за один раз объясняет систему, создаёт папку проекта со всеми нужными подпапками и подсказками, пров
- [[landing-status]] — Показывает текущее состояние: либо мастер-системы landing-system (какие скиллы и агенты установлены), либо активного проекта-лендинга (назва
- [[landing-visuals]] — Автоматически генерирует PNG-иконки и инфографику для лендинга через codex image_gen, встраивает их в `composed.html` вместо текстовых плейс
- [[landing-wireframe]] — Генерирует интерактивный HTML-файл, в котором каждый блок лендинга представлен в 2–3 вариантах оформления из библиотеки блоков. Маркетолог о

## 📐 Правила (стандарты качества)

- [[memory]] — Хранит автоматические записи всех разговоров с Claude Code в рамках одного лендинг-проекта. Папка ведётся машиной — вручную не редактируется
- [[premium-07b-checklist]] — Устанавливает обязательные стандарты качества для этапа 07b: агент `block-composer` собирает `composed.html` только при выполнении всех пунк
- [[wiki]] — Папка `wiki/` внутри каждого лендинг-проекта хранит авто-сгенерированный граф состояния проекта: текущий этап, выбранные блоки, бренд-цвета 
